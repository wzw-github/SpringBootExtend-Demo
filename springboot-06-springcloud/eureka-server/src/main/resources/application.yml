server:
  port: 8761
eureka:
  instance:
    hostname: eureka-server #eureka实例的主机名
  client:
    fetch-registry: false   #不从eureka上来获取服务的注册信息
    register-with-eureka: false   #不把自己注册到注册中心
    service-url:
      defaultZone: http://localhost:8761/eureka/  #eureka注册中心的地址
